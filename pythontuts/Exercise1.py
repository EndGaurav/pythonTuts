# Exercise
di = {
    "immutable": "cannot change",
    "mutable": "can change",
    "hostile": "aggresive",
    "tuple": "multiple elements"
}
user = input("Search your word: ")
print(di[user])
# print(di.get(user))
